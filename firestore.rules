rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // NOTE: These rules allow full read/write access to everyone.
    // This is enabled because the user requested an internal auth system 
    // without using Firebase Authentication services.
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
